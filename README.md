# 1. Тема и целевая аудитория

## Описание

Проектируемая система — публичная AI API-платформа, предоставляющая доступ к большим языковым моделям (LLM).

**Основная проектируемая функциональность — потоковая передача ответа модели (Streaming) через Server-Sent Events (SSE).**

В рамках проекта архитектура системы проектируется с фокусом на обработку большого количества долгоживущих HTTP-соединений и передачу данных частями по мере генерации токенов.

Реальные аналоги:  
OpenAI API, Anthropic API, Google Gemini API, Azure OpenAI Service.

---

## Выделяемая ключевая фича

Streaming-режим:

- Ответ модели передаётся частями (token-by-token)
- Соединение удерживается до завершения генерации
- Используется протокол `text/event-stream`
- Возникает большое количество одновременных долгоживущих соединений

Streaming является основной нагрузкообразующей функцией системы и определяет архитектурные решения.

---

## Целевая аудитория

**Местоположение:** глобальный рынок (Северная Америка, Европа, Азия).

### Размер аудитории (данные по ChatGPT)

По данным *DemandSage*:

- **Weekly Active Users (WAU): ~800 млн** — еженедельных активных пользователей ChatGPT, отражающих широкий глобальный охват. [1]  
- **Оценочная Monthly Active Users (MAU): <1 млрд** — оценка на основе WAU и месячной активности. [1]  
- **Оценочная Daily Active Users (DAU): ~114 млн** — оценка ежедневных уникальных пользователей. [1]

Эти данные отражают активную базу пользователей AI-сервисов и используются для проектирования инфраструктуры целевой AI API-платформы.

---

## Требования к функционалу

### Ключевые функции

- Создание запроса на генерацию текста (Chat Completion API)  
- Потоковая передача результата по мере генерации (SSE) — **основная фича**  
- Аутентификация по API-ключу  
- Ограничение частоты запросов (Rate Limiting)  
- Подсчёт и биллинг использованных токенов

---

## Ключевые продуктовые решения

- API является stateless — история диалога передаётся клиентом в каждом запросе.  
- Streaming реализуется через SSE поверх HTTP/1.1 или HTTP/2.  
- Inference-кластер (GPU) отделён от API-слоя.  
- Горизонтальное масштабирование API-нод.  
- Глобальная балансировка нагрузки.  
- Сервис работает круглосуточно (24/7).

---

# Список источников

1. DemandSage. *ChatGPT Statistics, User Growth & Usage.* https://www.demandsage.com/chatgpt-statistics/
